#include <stdio.h>
#include "main.h"
/**
 *_abs - Computes the absolute value
 *The integer for which to compute
 *Return: The absolute value of n.
 */
int _abs(int n)
{
if (n < 0)
return (-n);
else
return (n);
}
